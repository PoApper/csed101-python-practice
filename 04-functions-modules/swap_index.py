def swap_indices(ls, i, j):
    """
    Swap ls[i] and ls[j].

    >>> ls = [0, 1, 2, 3]
    >>> swap_indices(ls, 0, 2)
    >>> ls
    [2, 1, 0, 3]
    >>> swap_indices(ls, 1, 2)
    >>> ls
    [2, 0, 1, 3]
    """
    pass


if __name__ == "__main__":
    import doctest

    doctest.testmod(verbose=True)
